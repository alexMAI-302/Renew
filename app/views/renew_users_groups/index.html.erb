<% content_for :stylesheets_links do %>
	<link type="text/css" rel="stylesheet" media="screen" href="/stylesheets/main.css">
<% end %>
<% content_for :title do %>
Группы пользователей
<% end %>
<% content_for :content do %>
	<div>
		<% form_tag do%>
			<fieldset>
				<legend>Фильтр</legend>
				<nobr>
					<label for="renew_groups">Группа: </label>
					<%= select_tag(:renew_groups, options_for_select([["<Вce>",0]] + @renew_groups.collect {|p| [p.name, p.id]}, :selected => @selected_group)) %>
					<label for="renew_users"> Пользователь: </label>
					<%= select_tag(:renew_users, options_for_select([["<Вcе>",0]] + @renew_users.collect {|p| [p.name, p.id]}, :selected => @selected_user)) %>
				</nobr>
				<%= submit_tag "Применить"%>
			</fieldset>
		<% end %> 
	</div>
	<br>

	<% form_tag :action => 'save' do %>
		<table>
			<thead>
			  <tr>
				<th>
					Группа
				</th>
				<th>
					Пользователь
				</th>
				<th>
				</th>
			  </tr>
			</thead>

			<tbody>
				<% @renew_users_groups.each do |item| %>
					<tr>
						<td><%= select_tag(
							'renew_users_groups['+item.renew_user_group.id.to_s+']['+item.renew_user.id.to_s+'][group]',
							options_for_select(@renew_groups.collect {|p| [p.name, p.id]}, :selected=>item.renew_user_group.id )) %></td>
						<td><%= select_tag(
							'renew_users_groups['+item.renew_user_group.id.to_s+']['+item.renew_user.id.to_s+'][user]',
							options_for_select(@renew_users.collect {|p| [p.name, p.id]}, :selected=>item.renew_user.id )) %></td>
						<td><%= link_to("Удалить",
							{:action => "destroy", :group_id=>item.renew_user_group.id, :user_id=>item.renew_user.id},
							:confirm => 'Вы уверены? Удалить ?')%></td>
					</tr>
				<% end %>
			</tbody>
		</table>
		<%= submit_tag(value = "Сохранить") %>
	<% end %>
	<br/>
	Новая запись
	<% form_tag :action => 'create' do %>
		<div>
			<%= select_tag(
				'renew_users_group[group]',
				options_for_select(@renew_groups.collect {|p| [p.name, p.id]})) %>
			<%= select_tag(
				'renew_users_group[user]',
				options_for_select(@renew_users.collect {|p| [p.name, p.id]})) %>
		<%= submit_tag(value = "Создать")%></div>
	<% end %>

<% end %>